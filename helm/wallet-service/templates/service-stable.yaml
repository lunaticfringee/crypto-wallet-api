apiVersion: v1
kind: Service
metadata:
  name: {{ include "wallet-service.fullname" . }}-stable
spec:
  selector:
    {{- include "wallet-service.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http