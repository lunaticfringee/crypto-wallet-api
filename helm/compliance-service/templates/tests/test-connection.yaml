apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "compliance-service.fullname" . }}-test-connection"
  labels:
    {{- include "compliance-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "compliance-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
